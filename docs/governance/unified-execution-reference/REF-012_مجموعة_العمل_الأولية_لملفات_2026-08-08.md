# REF-012 — مجموعة العمل الأولية لملفات 8 أغسطس 2026

## الغرض

هذا السجل يحدد طريقة التعامل الأولية مع ملفات 8 أغسطس ضمن **المرجع التنفيذي الموحد لمشروع TransportERP**.

لا يمنح هذا السجل أي ملف حالة Current Approved — الحالي المعتمد من تلقاء نفسه.

## 1. عائلة GEN-016

الملف:

`الجدول_الحاكم_GEN-016_إعدادات_التشغيل_العامة_والمتغيرات_المشتركة_2026-08-08.md`

له نسخة موجودة داخل:

`/توحيد الخصائص العامة/`

وتوجد نسخة أخرى باسم ينتهي بـ`(1)` ثبت أنها مطابقة بايتيًا.

### التصنيف الأولي

- النسخة داخل المسار المنظم: `WORKING/REFERENCE CANDIDATE — مرشح عمل/مرجع` إلى أن تكتمل مراجعة السلطة.
- النسخة المطابقة `(1)`: `DUPLICATE COPY — نسخة مكررة`.
- لا حذف قبل استكمال Provenance — المصدرية وسجل الأصول.

## 2. عائلة ملاحظات هندسة الشاشات والخصائص

ظهرت عدة مواد بالاسم نفسه أو اسم قريب مع أحجام ومحتويات مختلفة.

هذا يعني أنها ليست مجموعة Duplicate — تكرارات بسيطة، بل على الأرجح Version Lineage — سلسلة إصدارات/تطور محتوى.

### التصنيف الأولي

- النسخ الصغيرة والأقدم: `HISTORICAL WORKING MATERIAL — مادة عمل تاريخية` مؤقتًا.
- النسخة الموجودة داخل `/توحيد الخصائص العامة/` والأكبر حجمًا: `LATEST LOCATED WORKING CANDIDATE — أحدث مرشح عمل تم العثور عليه` فقط، وليس Current Approved.
- المطلوب قبل أي ترقية: مقارنة محتوى، تحديد مصدر، واستخراج Change Delta — فرق التغيير.

## 3. الصور ذات النسخ المطابقة

ثبت وجود مجموعات صور متطابقة بايتيًا، منها:

- `950070.jpg` و`950070(1).jpg`
- `950071.jpg` و`950071(1).jpg`
- `950072.jpg` و`950072(1).jpg`

### التصنيف الأولي

`VISUAL DUPLICATE COPIES — نسخ بصرية مكررة`

لكنها تبقى محفوظة حتى يكتمل:

- Source URL — رابط المصدر إن وجد.
- Rights/License — الحقوق/الترخيص.
- Capture/Import Date — تاريخ الالتقاط/الاستيراد.
- Related Screen/Capability — الشاشة/القدرة المرتبطة.
- Canonical Visual Selection — اختيار النسخة البصرية المرجعية.

## 4. الملفات البحثية والبصرية

أي Screenshot / Image / Review Image — لقطة/صورة/صورة مراجعة لا تصبح Requirement — متطلبًا حاكمًا بمجرد وجودها.

تصنيفها الافتراضي:

`REFERENCE / EVIDENCE / RESEARCH — مرجع / دليل / بحث`

إلى أن يتم ربطها بقرار أو متطلب معتمد.

## 5. قاعدة العمل

كل عائلة ملفات تمر بالمراحل التالية:

1. Inventory — جرد.
2. Byte Identity — هوية بايتية.
3. Version Lineage — سلسلة إصدار.
4. Authority Classification — تصنيف السلطة.
5. Semantic Comparison — مقارنة دلالية.
6. Working Set Decision — قرار مجموعة العمل.
7. Archive/Duplicate Disposition — قرار الأرشفة/التكرار.

## 6. ما يمنع حاليًا

- لا حذف.
- لا overwrite — استبدال صامت.
- لا ترقية لنسخة حديثة لمجرد أنها أحدث.
- لا دمج لملفين مختلفين في البصمة قبل المقارنة الدلالية.

## 7. الخطوة التالية

الانتقال من الجرد الزمني إلى جرد العائلات ذات السلطة الأعلى:

1. Current Approved References — المراجع الحالية المعتمدة.
2. Unified Book / Kurrasa — الكراسة الموحدة.
3. Screen Registers — سجلات الشاشات.
4. Field Specifications — مواصفات الحقول.
5. Action/State/Permission Matrices — مصفوفات الإجراءات/الحالات/الصلاحيات.
6. Data/API/CoreUI contracts — عقود البيانات/واجهات البرمجة/واجهة المستخدم الأساسية.

ثم ربطها كلها داخل Master Artifact Register — سجل الأصول الرئيسي.