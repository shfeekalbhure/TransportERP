# GEN-003 — الدول — Canonical Screen Specification

**English:** Countries  
**Profile / Variant:** `MasterData / Standard`  
**CurrentDesignState:** `DESIGN_APPROVED`  
**OwnerTeam:** `DESIGN-LEAD / ORCHESTRATOR`  
**Batch:** `BATCH-03`

## Authority
- Owner issuance: `SRC-055 / OWNER-GEN003-EXTENDED-FIELD-AND-DTO-ISSUANCE-001`.
- W3: `GEN003_TYPED_SCREENDEFINITION_FULL_2026-08-22.md`.
- W1: `GEN003_W1_FULL_LOGICAL_FIELD_TRACE_2026-08-22.md`.
- W2: `GEN003_W2_FULL_FIELD_CONTRACT_2026-08-22.md`.
- Test input: `GEN003_FULL_ACCEPTANCE_TEST_SPECIFICATIONS_2026-08-22.md` — issued, runtime not run.

## ANALYSIS — TEAM-D01 PASS
Purpose: maintain the global country reference master used by downstream geography and business lookups.

Capabilities:
- View/Search → `GEN003.View`.
- Create → `GEN003.Create`.
- Edit → `GEN003.Edit` with expectedVersion.
- Disable → `GEN003.Disable` with reason + expectedVersion.
- Print → `GEN003.Print` preserving current query/filter/sort context.

Fields:
- `Code` required, create/edit.
- `ArabicName` required, create/edit.
- `EnglishName` optional, create/edit.
- `NationalityName` optional, create/edit.
- `ISO2` required, create/edit.
- `ISO3` optional, create/edit.
- `DialingCode` optional, create/edit.
- `Notes` optional multiline, create/edit.
- `Status` read-only `Active|Stopped` projection.
- `Version` hidden technical concurrency token.

## LAYOUT — TEAM-D02 PASS
Shared `MasterData / Standard` authority:
- MainData = `Content`, max two columns.
- Search = `Content`.
- MasterListGrid = `Fill`.
- Pagination = shared fixed region.
- Audit = shared CoreUI region.
- no Tabs, no LocalException, no local sizing/styling.

## FIELD_GRID — TEAM-D03 PASS
`AutoGenerateColumns=false`, `SelectionPolicy=SingleRow`, `UsesServerPaging=true`.

Grid columns in order:
1. `Code` — الرمز — text/code — content width.
2. `ArabicName` — الاسم العربي — text — primary Fill.
3. `EnglishName` — الاسم الإنجليزي — text — Fill/content.
4. `ISO2` — ISO2 — short text — content.
5. `ISO3` — ISO3 — short text — content.
6. `DialingCode` — مفتاح الاتصال — text — content.
7. `Status` — الحالة — enum/read-only — content state.

`NationalityName` and `Notes` are intentionally not default grid columns.
Search: `SearchText` across Code/ArabicName/EnglishName + optional `Status`; sorting only through W2 allow-list.

## UX — TEAM-D04 PASS
- create/edit availability is advisory and permission-bound; server is authoritative.
- disable is a controlled state change requiring reason and current version; no direct Status editor.
- stale version uses shared concurrency conflict + Reload/Refresh; no silent overwrite.
- validation/loading/error/empty/paging use shared CoreUI presenters.
- Print preserves the active query/filter/sort context; no client recomputation.
- no Delete, Enable, Activate, offline write or queue.

## VISUAL — TEAM-D05 PASS
Use only shared MasterData CoreUI tokens and presenters: RTL, DPI, typography, spacing, state/error/focus/required/read-only rendering, toolbar, grid, pagination and audit. No local colors, pixel widths/heights, custom toolbar or grid clone.

## Acceptance criteria
1. `MasterData / Standard` only.
2. exact issued fields preserved; Status is read-only projection.
3. seven explicit default grid columns; no autogenerated columns.
4. Print exists only because GEN003 current contract issues it.
5. server paging/allow-listed sort/shared error contract preserved.
6. no API/DTO/permission/DDL/offline invention.

## INDEPENDENT REVIEW — TEAM-D06 PASS
Cross-check against `GEN003_FULL_ACCEPTANCE_TEST_SPECIFICATIONS_2026-08-22.md` confirmed List/Create/Update/Disable/Print, concurrency, server security and prohibited Delete/Enable/Activate/Offline/Queue behavior. Open design findings: `0`.

Runtime tests remain `NOT RUN`; design approval is not runtime PASS.
