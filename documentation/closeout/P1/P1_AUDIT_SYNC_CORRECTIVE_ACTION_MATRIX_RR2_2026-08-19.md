# مصفوفة الإجراءات التصحيحية — إعادة المراجعة الثانية

**المشروع:** TransportERP  
**سبب الإصدار:** قرار المراجعة المستقلة `FAIL` على الالتزام `8af9af39f25e099e2f4b8ed4526997a907ba7602`  
**الفرع التصحيحي:** `feature/p1-audit-sync-production-20260819`  
**الحالة:** تنفيذ مطلوب/قيد الإغلاق؛ لا PR ولا دمج قبل PASS.

| المعرف | الملاحظة الحرجة | الإجراء الإلزامي | دليل الإغلاق المطلوب | حالة التنفيذ الحالية |
|---|---|---|---|---|
| RR-CR-01 | غياب Trigger PostgreSQL لـAuditEvent append-only | إبقاء وظيفة `prevent_audit_event_mutation()` وTrigger قبل UPDATE/DELETE على `transport_erp.audit_events` ضمن Migration قابلة للتطبيق | SQL Migration، فحص `pg_trigger`، واختبار مباشر يثبت رفض UPDATE وDELETE | منفذ في الفرع؛ يلزم من المراجع إعادة التشغيل |
| RR-CR-02 | غياب خدمة AuditEvent الإنتاجية وAPI القراءة | تثبيت `AuditEventService` للإلحاق وHash-chain والفلترة والتحقق والتصدير، مع تسجيله في طبقة التشغيل عند اعتماد المصادقة الفعلية | ملفات الخدمة، اختبار Hash-chain جديد، اختبار الفلاتر والعزل، ومسار قراءة موثق | منفذ في الفرع؛ يلزم فحص API/المصادقة الفعلية |
| RR-CR-03 | غياب ConflictCase مستقل | تثبيت كيان وجدول `conflict_cases` وعلاقة FK وفهرس فريد وقيود الحالة وحقول snapshots/resolution | Migration، DbContext، entity، اختبار الإنشاء والحسم والعلاقة | منفذ في الفرع؛ يلزم إعادة تطبيق Migration |
| RR-CR-04 | غياب Exponential Backoff وmax attempts | تثبيت سياسة Backoff، إبقاء الحالة FAILED أثناء الإعادة، تحديث RetryCount وNextRetryAt، وتحويل الاستنفاد إلى REJECTED | اختبار قيم RetryCount/NextRetryAt، اختبار الاستنفاد، وإثبات عدم وجود RETRIED | منفذ في الفرع؛ يلزم مراجعة قيم السياسة مقابل العقد |
| RR-CR-05 | غياب API `POST /api/v1/sync/operations:batch` | تثبيت Minimal API مع DTO ونتيجة مستقلة لكل عملية واشتقاق النطاق من Claims الخادم وتسجيل النتائج | بناء API، اختبار HTTP/contract، وإثبات عدم قبول Company/Branch/User/Device من العميل كمصدر ثقة | منفذ في الفرع؛ يلزم اختبار HTTP بموفر هوية فعلي |
| RR-CR-06 | غياب تحقق device/user/company/branch/permission | تثبيت `SyncSecurityContext` وفحص الجهاز المسجل، المستخدم، الشركة، الفرع، والصلاحية على الخادم | اختبارات رفض الجهاز غير المسجل والصلاحية والشركة/الفرع والمستخدم المختلف | منفذ في الفرع؛ يلزم مراجعة Claims provider قبل PASS |
| RR-CR-07 | غياب دليل PostgreSQL وMigration up/down/reapply | توفير بيئة PostgreSQL 18.6 أو سجل CI قابل للربط، وتطبيق قاعدة فارغة ثم Down ثم Up ثم أحدث Migration | سجل أوامر كامل، أسماء القواعد، نسخة PostgreSQL، وجرد الجداول/الفهارس/triggers | منفذ محليًا؛ سجل الإعادة مرفق في الحزمة، ويلزم إعادة تحقق مستقل |
| RR-CR-08 | عدم قابلية التحقق من 34/34 و3/3 | ربط نتائج الاختبارات بالفرع والالتزام وسجل كامل داخل الحزمة، وعدم الاكتفاء بملخص | test log كامل، أمر التشغيل، عدد الاختبارات، وبيئة التنفيذ | سجل 34/34 و3/3 محفوظ؛ يلزم المراجع إعادة التشغيل |

## بوابة الإغلاق

لا يعتبر أي بند مغلقًا نهائيًا إلا بعد تحقق المراجع المستقل من الدليل القابل لإعادة التشغيل وتوثيق النتيجة. لا يكفي وجود الكود أو Migration نصيًا دون تشغيل PostgreSQL، ولا يكفي نجاح Baseline المحايد لإثبات الخدمة الإنتاجية.

## قاعدة النشر

يُسمح بنشر فرع التصحيح للمراجعة فقط. لا يُفتح Pull Request للمراجعة البرمجية ولا يُدمج في `master` قبل إصدار تقرير مستقل جديد بقرار `PASS`.
