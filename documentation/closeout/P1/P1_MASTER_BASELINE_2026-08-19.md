# TransportERP — P1 Master Baseline

**Release_ID:** `P1-PLATFORM-SETTINGS-ACCOUNTING-2026-08`  
**حالة الإصدار:** `CLOSED_AFTER_MERGE`  
**مرجع master:** `e8e22de26b4faa5040f53582ab2c8934d43216f0`  
**الـhead الذي سبّق الدمج واجتاز RR7:** `d39dfaf3596548c4ed0d1f66ec75c5faafc55509`  
**PR:** `#33`  
**تاريخ التثبيت:** 2026-08-19 UTC+3

## 1. غرض baseline

يثبت هذا السجل النسخة المرجعية التي يبدأ منها أي عمل لاحق. لا يجوز تعديل نطاق P1 أو إعادة تفسير أدلته داخل P2. أي تغيير على هذه النسخة يجب أن يأتي عبر فرع وPR مستقلين، مع مراجعة مستقلة وقرار `PASS` صريح قبل الدمج.

## 2. نطاق P1 المثبت

يشمل P1 المنصة الأساسية، الهوية، المستخدمين، الأدوار، الصلاحيات، تعدد الشركات والفروع، الإعدادات العامة وإعدادات الشركة والفرع والحسابات، الحسابات الأساسية، التدقيق، والمزامنة الأساسية Offline/Online. لا يشمل P1 التشغيل التفصيلي للبوالص والشحن، التوصيل، النقل، المشاوير، السفريات والتذاكر، الأسطول وGPS، الصيانة، الموارد البشرية، الجمارك، أو تكاملات المزودين الخارجية؛ وهذه المجالات مؤجلة لإصدارات مستقلة حسب وثيقة نطاق P1.

## 3. النتائج المثبتة عند baseline

| البند | النتيجة |
|---|---|
| حالة PR #33 | `MERGED` |
| merge commit | `e8e22de26b4faa5040f53582ab2c8934d43216f0` |
| مطابقة شجرة الدمج للـhead المعتمد | `PASS` |
| الاختبارات الافتراضية | `41/41 PASS` |
| Failed / Skipped | `0 / 0` |
| Build warnings / errors | `0 / 0` |
| قاعدة البيانات | PostgreSQL 18.6 |
| مخطط PostgreSQL | `transport_erp` |
| جداول P1 الفيزيائية | 21 جدولًا كما هو موثق في Physical Schema |
| دورة Migration المستقلة | `Down → Up → Reapply PASS` |
| AuditEvent append-only | مثبتة بخدمة وTrigger PostgreSQL واختبار |
| SyncOperation | idempotency وscope وRetry Backoff وConflictCase مثبتة |
| حالة RETRIED | ليست حالة تشغيلية مستقلة؛ تبقى العملية `FAILED` أثناء Retry |
| فحص الأسرار | لا تطابقات في الملفات المتتبعة |
| فحص NuGet | لا حزم ضعيفة وفق المصادر المستخدمة |

## 4. الأدلة الحاكمة

تعتمد هذه النسخة على نطاق P1، وعقود W1/W2/W3، وعقد المزامنة، ووثيقة Physical Schema، وتقرير التدقيق العميق بعد الدمج وحزمة أدلته. لا يجوز اعتبار ملفات السجلات التاريخية القديمة دليلًا على head مختلف؛ كل دليل تنفيذي جديد يجب أن يثبت SHA الذي نُفذ عليه.

## 5. قاعدة التغيير بعد baseline

أي تعديل على P1 أو إضافة تشغيلية جديدة يتطلب: فرعًا مستقلًا، عقدًا أو ملحق تغيير، اختبارات قبول، فحصًا أمنيًا، فحصًا لتعدد الشركات والفروع، أدلة قابلة لإعادة التشغيل، مراجعة مستقلة، وقرار `PASS` صريح. لا يكفي `PASS_WITH_NOTES` للتفويض بالدمج.

## 6. حدود الانتقال إلى P2

يبدأ P2 من هذا baseline ولا يعيد فتح P1. يجب أن يحدد كل مسار P2 مجالًا تشغيليًا واحدًا أو مجموعة مترابطة معتمدة كتابيًا، ثم يغلق عقود البيانات والأفعال والشاشات والاختبارات قبل تنفيذ الشيفرة. لا يجوز إنشاء جداول أو شاشات تشغيلية لمجال P2 اعتمادًا على الذاكرة أو التخمين.
