# P1 Master Evidence Package

هذه الحزمة مرتبطة حصريًا بـ`master` عند merge commit `e8e22de26b4faa5040f53582ab2c8934d43216f0`، وبـhead السابق المعتمد `d39dfaf3596548c4ed0d1f66ec75c5faafc55509`.

تتضمن السجلات الخام لحالة GitHub، المطابقة، traceability، البناء، الاختبارات، فحص الأسرار، NuGet، ودورة Migration. ملف `REPO_EVIDENCE_MANIFEST_SHA256.txt` يثبت بصمات الملفات الموجودة في هذه الحزمة. لا تُقبل سجلات مؤقتة خارج هذه الحزمة كمرجع وحيد للإغلاق.

أي إعادة تنفيذ لاحقة يجب أن تنشئ مجلد أدلة جديدًا مرتبطًا بالـSHA الجديد، لا أن تستبدل هذه الحزمة التاريخية.
