# CURRENT DIRECTIVE — TEAM-C1

`STOP`

Preserve sealed v1.0 unchanged. TEAM-E proved that v1.0 incorrectly states that `TransportErpDbContextFactory` has a source-coded local fallback; direct source fails closed when `TRANSPORTERP_DESIGN_CONNSTR` is absent.

Control Tower verified all 14 detached hashes, factual correction, manifest, seal, and handoff. v1.1 is the governing TEAM-C1 input; v1.0 remains preserved and superseded for downstream use. Do not modify either package without a new `REOPEN`.
